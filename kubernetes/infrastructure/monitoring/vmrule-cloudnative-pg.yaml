---
# CNPG's own sample alerts, as a VMRule (the PrometheusRule CRD is not installed).
# Source: cloudnative-pg/cloudnative-pg docs/src/samples/monitoring/prometheusrule.yaml
apiVersion: operator.victoriametrics.com/v1beta1
kind: VMRule
metadata:
  name: cloudnative-pg
spec:
  groups:
    - name: cnpg-default.rules
      rules:
        - alert: LongRunningTransaction
          expr: cnpg_backends_max_tx_duration_seconds > 300
          for: 1m
          labels: { severity: warning }
          annotations:
            summary: A query is taking longer than 5 minutes.
            description: Pod {{ $labels.pod }} is taking more than 5 minutes for a query.
        - alert: BackendsWaiting
          expr: cnpg_backends_waiting_total > 300
          for: 1m
          labels: { severity: warning }
          annotations:
            summary: A backend has been waiting longer than 5 minutes.
            description: Pod {{ $labels.pod }} has been waiting for longer than 5 minutes.
        - alert: PGDatabaseXidAge
          expr: cnpg_pg_database_xid_age > 300000000
          for: 1m
          labels: { severity: warning }
          annotations:
            summary: Transactions from the frozen XID to the current one.
            description: Over 300,000,000 transactions from frozen xid on pod {{ $labels.pod }}.
        - alert: PGReplication
          expr: cnpg_pg_replication_lag > 300
          for: 1m
          labels: { severity: warning }
          annotations:
            summary: The standby is lagging behind the primary.
            description: Standby is lagging behind by over 300 seconds (5 minutes).
        - alert: LastFailedArchiveTime
          # last_failed_time is -1 until archiving has ever failed, so this stays
          # far negative on a healthy cluster.
          expr: (cnpg_pg_stat_archiver_last_failed_time - cnpg_pg_stat_archiver_last_archived_time) > 1
          for: 1m
          labels: { severity: warning }
          annotations:
            summary: WAL archiving is failing.
            description: Archiving failed for {{ $labels.pod }}.
        - alert: DatabaseDeadlockConflicts
          expr: cnpg_pg_stat_database_deadlocks > 10
          for: 1m
          labels: { severity: warning }
          annotations:
            summary: Too many database conflicts.
            description: There are over 10 deadlock conflicts in {{ $labels.pod }}.
        - alert: ReplicaFailingReplication
          expr: cnpg_pg_replication_in_recovery > cnpg_pg_replication_is_wal_receiver_up
          for: 1m
          labels: { severity: warning }
          annotations:
            summary: The replica is failing to replicate.
            description: Replica {{ $labels.pod }} is failing to replicate.
