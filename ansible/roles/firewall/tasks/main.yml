---
- name: Install ufw
  ansible.builtin.apt:
    name: ufw
    state: present

- name: Set default incoming policy
  community.general.ufw:
    direction: incoming
    policy: "{{ firewall_default_incoming }}"

- name: Set default outgoing policy
  community.general.ufw:
    direction: outgoing
    policy: "{{ firewall_default_outgoing }}"

- name: Remove superseded ufw rules
  community.general.ufw:
    rule: allow
    port: "{{ item.port | string }}"
    proto: "{{ item.proto }}"
    from_ip: "{{ item.from | default('any') }}"
    delete: true
  loop: "{{ firewall_absent_rules | default([]) }}"
  loop_control:
    label: "delete {{ item.port }}/{{ item.proto }} from {{ item.from | default('any') }}"

- name: Trust the Tailscale interface
  community.general.ufw:
    rule: allow
    direction: in
    interface: "{{ firewall_tailnet_iface }}"
  when: firewall_tailnet_iface | length > 0

- name: Allow configured ports
  community.general.ufw:
    rule: allow
    port: "{{ item.port | string }}"
    proto: "{{ item.proto }}"
    from_ip: "{{ item.from | default('any') }}"
    comment: "{{ item.comment | default(omit) }}"
  loop: "{{ firewall_allow_rules }}"
  loop_control:
    label: "{{ item.port }}/{{ item.proto }} from {{ item.from | default('any') }}"

- name: Enable ufw
  community.general.ufw:
    state: enabled
